// Re-export Supabase service as apiService for compatibility with existing components
export { supabaseService as apiService } from './supabase';
export { supabaseService as default } from './supabase';